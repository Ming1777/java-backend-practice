# 2026-09-09 学习记录

记录于 2026-09-10，根据学习对话和现有代码整理。

## 项目：JUnit、Mockito、MockMvc

- 运行 firstTest，观察预期 2 时通过、预期 3 时失败，理解预期值与实际值。
- 在指导下完成 Mapper mock、用户存在时的 Service 测试；用户确认运行通过。
- 用户不存在时的测试由助手应要求补写：assertThrows 检查 BusinessException，再检查错误码、提示和两分钟空值缓存。尚未独立复写。
- 运行 Controller 的 MockMvc 测试通过：GET /users、HTTP 状态、业务 code、列表内容和 Service 调用。
- 理解 standaloneSetup 使用真实 Controller 和 MVC 请求处理机制，Service 被模拟，不验证真实数据库 SQL 或真实网络服务。
- 2026-09-10 助手重新运行两个测试类：5 个测试全部通过。这是代码验证，不代表独立掌握。

## 算法：二分查找

- 初次编写混淆下标与元素、右边界和循环；提示后完成 BinarySearchReview0909。
- 用户截图验证数组 [-1,0,3,5,9,12] 查找 0 返回下标 1。
- 空数组、首尾元素、单元素、不存在目标等边界尚未确认由用户验证。
- 待改进：将 (left + right) / 2 改成 left + (right - left) / 2，并解释整数溢出风险。
- 独立闭卷复写待检查。

## 面试复习

已提供测试组织方式、mock 的边界、JUnit/Mockito/MockMvc 区别、when/verify、assertThrows、HTTP 状态与业务码、二分复杂度的讲解。尚未逐题独立复述验收。

用户提出“抛出的异常被接住了所以能通过？”；继续巩固：必须抛出预期类型或子类，且后续断言通过，不能理解为任意异常被接住就通过。

## 后续学习方式

用户担心只看懂不会手写，约定从 9 月 10 日起：先小段填空，再独立写几行，再隔日复写；卡住先给提示，避免直接提供完整答案。助手完成的代码不计为用户独立完成。
